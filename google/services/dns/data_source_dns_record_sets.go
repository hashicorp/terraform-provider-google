// Copyright IBM Corp. 2014, 2026
// SPDX-License-Identifier: MPL-2.0
// ----------------------------------------------------------------------------
//
//	***     AUTO GENERATED CODE    ***    Type: Handwritten     ***
//
// ----------------------------------------------------------------------------
//
//	This code is generated by Magic Modules using the following:
//
//	Source file: https://github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/third_party/terraform/services/dns/data_source_dns_record_sets.go
//
//	DO NOT EDIT this file directly. Any changes made to this file will be
//	overwritten during the next generation cycle.
//
// ----------------------------------------------------------------------------
package dns

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	"github.com/hashicorp/terraform-provider-google/google/registry"
	"github.com/hashicorp/terraform-provider-google/google/tpgresource"
	transport_tpg "github.com/hashicorp/terraform-provider-google/google/transport"
	"google.golang.org/api/dns/v1"
)

func DataSourceDnsRecordSets() *schema.Resource {
	return &schema.Resource{
		Read: dataSourceDnsRecordSetsRead,

		Schema: map[string]*schema.Schema{
			"managed_zone": {
				Type:     schema.TypeString,
				Required: true,
			},

			"type": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"rrsets": {
				Type:     schema.TypeList,
				Computed: true,
				Elem: &schema.Resource{
					Schema: map[string]*schema.Schema{
						"name": {
							Type:     schema.TypeString,
							Computed: true,
						},
						"type": {
							Type:     schema.TypeString,
							Computed: true,
						},
						"ttl": {
							Type:     schema.TypeInt,
							Computed: true,
						},
						"rrdatas": {
							Type:     schema.TypeList,
							Computed: true,
							Elem: &schema.Schema{
								Type: schema.TypeString,
							},
						},
					},
				},
			},

			"project": {
				Type:     schema.TypeString,
				Optional: true,
			},
		},
	}
}

func dataSourceDnsRecordSetsRead(d *schema.ResourceData, meta interface{}) error {
	config := meta.(*transport_tpg.Config)

	userAgent, err := tpgresource.GenerateUserAgentString(d, config.UserAgent)
	if err != nil {
		return err
	}

	project, err := tpgresource.GetProject(d, config)
	if err != nil {
		return err
	}

	zone := d.Get("managed_zone").(string)

	req := NewClient(config, userAgent).ResourceRecordSets.List(project, zone)

	name := d.Get("name").(string)
	recordType := d.Get("type").(string)

	if name != "" {
		req.Name(name)
		if recordType != "" {
			req.Type(recordType)
		}
	}

	var rrsets []*dns.ResourceRecordSet

	err = req.Pages(context.Background(), func(page *dns.ResourceRecordSetsListResponse) error {
		for _, r := range page.Rrsets {
			if recordType != "" && name == "" {
				if r.Type != recordType {
					continue
				}
			}
			rrsets = append(rrsets, r)
		}
		return nil
	})

	if err != nil {
		return fmt.Errorf("error reading DNS Record Sets: %w", err)
	}

	flattened := flattenDnsRecordSets(rrsets)

	if err := d.Set("rrsets", flattened); err != nil {
		return fmt.Errorf("error setting rrsets: %w", err)
	}

	d.SetId(fmt.Sprintf("projects/%s/managedZones/%s", project, zone))

	if err := d.Set("project", project); err != nil {
		return fmt.Errorf("error setting project: %w", err)
	}

	return nil
}

func flattenDnsRecordSets(rrsets []*dns.ResourceRecordSet) []map[string]interface{} {
	result := make([]map[string]interface{}, 0, len(rrsets))

	for _, r := range rrsets {
		m := map[string]interface{}{
			"name":    r.Name,
			"type":    r.Type,
			"ttl":     r.Ttl,
			"rrdatas": r.Rrdatas,
		}
		result = append(result, m)
	}

	return result
}

func init() {
	registry.Schema{
		Name:        "google_dns_record_sets",
		ProductName: "dns",
		Type:        registry.SchemaTypeDataSource,
		Schema:      DataSourceDnsRecordSets(),
	}.Register()
}
