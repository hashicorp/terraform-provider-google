// Copyright IBM Corp. 2014, 2026
// SPDX-License-Identifier: MPL-2.0

// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
//
// ----------------------------------------------------------------------------
//
//     This code is generated by Magic Modules using the following:
//
//     Configuration: https://github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/products/iap/AgentRegistryAgent.yaml
//     Template:      https://github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/templates/terraform/iam_policy.go.tmpl
//
//     DO NOT EDIT this file directly. Any changes made to this file will be
//     overwritten during the next generation cycle.
//
// ----------------------------------------------------------------------------

package iap

import (
	"fmt"
	"regexp"
	"strings"

	"github.com/hashicorp/errwrap"
	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	"google.golang.org/api/cloudresourcemanager/v1"

	"github.com/hashicorp/terraform-provider-google/google/registry"
	"github.com/hashicorp/terraform-provider-google/google/tpgiamresource"
	"github.com/hashicorp/terraform-provider-google/google/tpgresource"
	transport_tpg "github.com/hashicorp/terraform-provider-google/google/transport"
)

var (
	_ = regexp.Match
	_ = strings.Trim
	_ = errwrap.Wrap
	_ = schema.Noop
)

func init() {
	registry.Schema{
		Name:        "google_iap_agent_registry_agent_iam_binding",
		ProductName: "Iap",
		Type:        registry.SchemaTypeIAMResource,
		Schema:      tpgiamresource.ResourceIamBinding(IapAgentRegistryAgentIamSchema, IapAgentRegistryAgentIamUpdaterProducer, IapAgentRegistryAgentIdParseFunc),
	}.Register()
	registry.Schema{
		Name:        "google_iap_agent_registry_agent_iam_member",
		ProductName: "Iap",
		Type:        registry.SchemaTypeIAMResource,
		Schema:      tpgiamresource.ResourceIamMember(IapAgentRegistryAgentIamSchema, IapAgentRegistryAgentIamUpdaterProducer, IapAgentRegistryAgentIdParseFunc, tpgiamresource.IamWithParentResourceIdentity(IapAgentRegistryAgentIamParentParentResourceIdentityParser)),
	}.Register()
	registry.Schema{
		Name:        "google_iap_agent_registry_agent_iam_policy",
		ProductName: "Iap",
		Type:        registry.SchemaTypeIAMResource,
		Schema:      tpgiamresource.ResourceIamPolicy(IapAgentRegistryAgentIamSchema, IapAgentRegistryAgentIamUpdaterProducer, IapAgentRegistryAgentIdParseFunc),
	}.Register()
	registry.Schema{
		Name:        "google_iap_agent_registry_agent_iam_policy",
		ProductName: "Iap",
		Type:        registry.SchemaTypeIAMDataSource,
		Schema:      tpgiamresource.DataSourceIamPolicy(IapAgentRegistryAgentIamSchema, IapAgentRegistryAgentIamUpdaterProducer),
	}.Register()
}

var IapAgentRegistryAgentIamSchema = map[string]*schema.Schema{
	"project": {
		Type:     schema.TypeString,
		Computed: true,
		Optional: true,
		ForceNew: true,
	},
	"location": {
		Type:     schema.TypeString,
		Computed: true,
		Optional: true,
		ForceNew: true,
	},
	"agent_id": {
		Type:             schema.TypeString,
		Required:         true,
		ForceNew:         true,
		DiffSuppressFunc: tpgresource.CompareSelfLinkOrResourceName,
	},
}

type IapAgentRegistryAgentIamUpdater struct {
	project  string
	location string
	agentId  string
	d        tpgresource.TerraformResourceData
	Config   *transport_tpg.Config
}

func IapAgentRegistryAgentIamUpdaterProducer(d tpgresource.TerraformResourceData, config *transport_tpg.Config) (tpgiamresource.ResourceIamUpdater, error) {
	values := make(map[string]string)

	project, _ := tpgresource.GetProject(d, config)
	if project != "" {
		if err := d.Set("project", project); err != nil {
			return nil, fmt.Errorf("Error setting project: %s", err)
		}
	}
	values["project"] = project
	location, _ := tpgresource.GetLocation(d, config)
	if location != "" {
		if err := d.Set("location", location); err != nil {
			return nil, fmt.Errorf("Error setting location: %s", err)
		}
	}
	values["location"] = location
	if v, ok := d.GetOk("agent_id"); ok {
		values["agent_id"] = v.(string)
	}

	// We may have gotten either a long or short name, so attempt to parse long name if possible
	m, err := tpgresource.GetImportIdQualifiers([]string{"projects/(?P<project>[^/]+)/locations/(?P<location>[^/]+)/iap_web/agentRegistry/agents/(?P<agent_id>[^/]+)", "(?P<project>[^/]+)/(?P<location>[^/]+)/(?P<agent_id>[^/]+)", "(?P<location>[^/]+)/(?P<agent_id>[^/]+)", "(?P<agent_id>[^/]+)"}, d, config, d.Get("agent_id").(string))
	if err != nil {
		return nil, err
	}

	for k, v := range m {
		values[k] = v
	}

	u := &IapAgentRegistryAgentIamUpdater{
		project:  values["project"],
		location: values["location"],
		agentId:  values["agent_id"],
		d:        d,
		Config:   config,
	}

	if err := d.Set("project", u.project); err != nil {
		return nil, fmt.Errorf("Error setting project: %s", err)
	}
	if err := d.Set("location", u.location); err != nil {
		return nil, fmt.Errorf("Error setting location: %s", err)
	}
	if err := d.Set("agent_id", u.GetResourceId()); err != nil {
		return nil, fmt.Errorf("Error setting agent_id: %s", err)
	}

	return u, nil
}

func IapAgentRegistryAgentIdParseFunc(d *schema.ResourceData, config *transport_tpg.Config) error {
	values := make(map[string]string)

	project, _ := tpgresource.GetProject(d, config)
	if project != "" {
		values["project"] = project
	}

	location, _ := tpgresource.GetLocation(d, config)
	if location != "" {
		values["location"] = location
	}

	m, err := tpgresource.GetImportIdQualifiers([]string{"projects/(?P<project>[^/]+)/locations/(?P<location>[^/]+)/iap_web/agentRegistry/agents/(?P<agent_id>[^/]+)", "(?P<project>[^/]+)/(?P<location>[^/]+)/(?P<agent_id>[^/]+)", "(?P<location>[^/]+)/(?P<agent_id>[^/]+)", "(?P<agent_id>[^/]+)"}, d, config, d.Id())
	if err != nil {
		return err
	}

	for k, v := range m {
		values[k] = v
	}

	u := &IapAgentRegistryAgentIamUpdater{
		project:  values["project"],
		location: values["location"],
		agentId:  values["agent_id"],
		d:        d,
		Config:   config,
	}
	if err := d.Set("agent_id", u.GetResourceId()); err != nil {
		return fmt.Errorf("Error setting agent_id: %s", err)
	}
	d.SetId(u.GetResourceId())
	return nil
}

func (u *IapAgentRegistryAgentIamUpdater) GetResourceIamPolicy() (*cloudresourcemanager.Policy, error) {
	url, err := u.qualifyAgentRegistryAgentUrl("getIamPolicy")
	if err != nil {
		return nil, err
	}

	project, err := tpgresource.GetProject(u.d, u.Config)
	if err != nil {
		return nil, err
	}
	var obj map[string]interface{}
	obj = map[string]interface{}{
		"options": map[string]interface{}{
			"requestedPolicyVersion": tpgiamresource.IamPolicyVersion,
		},
	}

	userAgent, err := tpgresource.GenerateUserAgentString(u.d, u.Config.UserAgent)
	if err != nil {
		return nil, err
	}

	policy, err := transport_tpg.SendRequest(transport_tpg.SendRequestOptions{
		Config:    u.Config,
		Method:    "POST",
		Project:   project,
		RawURL:    url,
		UserAgent: userAgent,
		Body:      obj,
	})
	if err != nil {
		return nil, fmt.Errorf("Error retrieving IAM policy for %s: %w", u.DescribeResource(), err)
	}

	out := &cloudresourcemanager.Policy{}
	err = tpgresource.Convert(policy, out)
	if err != nil {
		return nil, fmt.Errorf("Cannot convert a policy to a resource manager policy: %w", err)
	}

	return out, nil
}

func (u *IapAgentRegistryAgentIamUpdater) SetResourceIamPolicy(policy *cloudresourcemanager.Policy) error {
	json, err := tpgresource.ConvertToMap(policy)
	if err != nil {
		return err
	}

	obj := make(map[string]interface{})
	obj["policy"] = json

	url, err := u.qualifyAgentRegistryAgentUrl("setIamPolicy")
	if err != nil {
		return err
	}
	project, err := tpgresource.GetProject(u.d, u.Config)
	if err != nil {
		return err
	}

	userAgent, err := tpgresource.GenerateUserAgentString(u.d, u.Config.UserAgent)
	if err != nil {
		return err
	}

	_, err = transport_tpg.SendRequest(transport_tpg.SendRequestOptions{
		Config:    u.Config,
		Method:    "POST",
		Project:   project,
		RawURL:    url,
		UserAgent: userAgent,
		Body:      obj,
		Timeout:   u.d.Timeout(schema.TimeoutCreate),
	})
	if err != nil {
		return fmt.Errorf("Error setting IAM policy for %s: %w", u.DescribeResource(), err)
	}

	return nil
}

func (u *IapAgentRegistryAgentIamUpdater) qualifyAgentRegistryAgentUrl(methodIdentifier string) (string, error) {
	urlTemplate := fmt.Sprintf("{{IapBasePath}}%s:%s", fmt.Sprintf("projects/%s/locations/%s/iap_web/agentRegistry/agents/%s", u.project, u.location, u.agentId), methodIdentifier)
	url, err := tpgresource.ReplaceVars(u.d, u.Config, urlTemplate)
	if err != nil {
		return "", err
	}
	return url, nil
}

func (u *IapAgentRegistryAgentIamUpdater) GetResourceId() string {
	return fmt.Sprintf("projects/%s/locations/%s/iap_web/agentRegistry/agents/%s", u.project, u.location, u.agentId)
}

func IapAgentRegistryAgentIamParentParentResourceIdentityParser(d *schema.ResourceData, identity *schema.IdentityData, transportConfig *transport_tpg.Config) (string, error) {
	return tpgiamresource.ParseIamResourceIdentity(d, identity, transportConfig, tpgiamresource.IamResourceIdentityConfig{
		Params: []tpgiamresource.IamIdentityParam{
			{Key: "project", IdentityKey: "project"},
			{Key: "location", IdentityKey: "location"},
			{Key: "agentId", IdentityKey: "agent_id"},
		},
		UriFormat: "projects/%s/locations/%s/iap_web/agentRegistry/agents/%s",
	})
}

func (u *IapAgentRegistryAgentIamUpdater) GetMutexKey() string {
	return fmt.Sprintf("iam-iap-agentregistryagent-%s", u.GetResourceId())
}

func (u *IapAgentRegistryAgentIamUpdater) DescribeResource() string {
	return fmt.Sprintf("iap agentregistryagent %q", u.GetResourceId())
}
